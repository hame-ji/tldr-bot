---
phase: 01-infrastructure-bot-setup
plan: 03
type: execute
wave: 2
depends_on:
  - "02"
files_modified:
  - .github/workflows/digest.yml
  - src/main.py
autonomous: false
requirements:
  - INFRA-03
  - BOT-01
  - BOT-02
  - BOT-03
user_setup:
  - service: telegram
    why: "Bot token and allowed chat ID must come from user's Telegram account"
    env_vars:
      - name: TELEGRAM_BOT_TOKEN
        source: "BotFather token"
      - name: TELEGRAM_CHAT_ID
        source: "Chat id captured from getUpdates"
  - service: gemini
    why: "API key required for later summarization phases"
    env_vars:
      - name: GEMINI_API_KEY
        source: "Google AI Studio API key"
must_haves:
  truths:
    - "Bot token is confirmed valid against Telegram API"
    - "Webhook is cleared so polling is not blocked"
    - "Allowed chat id is captured and configured as a secret"
  artifacts:
    - path: ".github/workflows/digest.yml"
      provides: "Environment variable wiring and setup verification step"
    - path: "src/main.py"
      provides: "Pipeline entrypoint stub used by workflow smoke run"
  key_links:
    - from: ".github/workflows/digest.yml"
      to: "TELEGRAM_BOT_TOKEN"
      via: "env mapping"
      pattern: "TELEGRAM_BOT_TOKEN"
    - from: ".github/workflows/digest.yml"
      to: "TELEGRAM getUpdates check"
      via: "curl command"
      pattern: "getUpdates"
---

<objective>
Wire required secrets into the workflow and complete live Telegram setup validation so polling prerequisites are proven before implementation phases.

Purpose: Close the human-in-the-loop setup gap and leave an executable checklist with automated API verification.
Output: Secret-aware workflow checks, entrypoint stub, and completed setup checkpoint evidence.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-infrastructure-bot-setup/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add workflow smoke checks for secret wiring and Telegram API validation</name>
  <files>.github/workflows/digest.yml, src/main.py</files>
  <action>Create `src/main.py` as a safe pipeline placeholder entrypoint and update workflow to map `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`, and `GEMINI_API_KEY` into environment variables. Add non-destructive API smoke checks for `getMe`, `deleteWebhook`, and `getUpdates` that fail loudly when credentials are missing or invalid.</action>
  <verify>
    <automated>python -m compileall src && python - <<'PY'\nimport pathlib\ny = pathlib.Path('.github/workflows/digest.yml').read_text()\nfor marker in ['TELEGRAM_BOT_TOKEN', 'TELEGRAM_CHAT_ID', 'GEMINI_API_KEY', 'getMe', 'deleteWebhook', 'getUpdates']:\n    assert marker in y, marker\nprint('secret-and-api-checks-wired')\nPY</automated>
  </verify>
  <done>Workflow and entrypoint include explicit secret wiring and Telegram API validation logic that can run in CI.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Complete credential provisioning and confirm live Telegram checks</name>
  <action>In GitHub repository settings, add `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`, and `GEMINI_API_KEY`. In Telegram, create bot via BotFather, send a message to the bot from the target chat, then run a manual workflow dispatch so the built-in `getMe`, `deleteWebhook`, and `getUpdates` checks execute with live credentials.</action>
  <verify>
    <automated>gh workflow run digest.yml --ref main</automated>
  </verify>
  <done>Workflow run for this phase succeeds with live secrets and Telegram API checks; chat id used in secrets matches the intended private chat.</done>
  <resume-signal>Reply `setup-complete` with the workflow run URL, or paste failing step output.</resume-signal>
</task>

</tasks>

<verification>
After user setup, dispatch workflow manually and confirm Telegram checks pass with live credentials.
</verification>

<success_criteria>
- Secrets exist in GitHub (`TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`, `GEMINI_API_KEY`)
- `getMe` succeeds with bot token
- `deleteWebhook` succeeds and polling is unblocked
- `getUpdates` returns messages from expected chat context
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-bot-setup/01-03-SUMMARY.md`
</output>
