---
phase: 04-gemini-summarization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/summarizer.py
  - src/main.py
  - tests/test_summarizer.py
autonomous: true
requirements:
  - SUM-04
  - SUM-05
must_haves:
  truths:
    - "Summaries are written to data/sources by run date"
    - "Rate-limit retry, spacing, and model fallback behavior exist in summarizer path"
    - "Summarization failures become durable failure records"
  artifacts:
    - path: "tests/test_summarizer.py"
      provides: "Regression coverage for summary write/failure behavior"
    - path: "src/main.py"
      provides: "Pipeline integration for summarization step"
---

<objective>
Integrate OpenRouter summarization into pipeline flow and add tests for output writing, transcript failure handling, and retry-aware behavior.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add summary file persistence and failure fallback handling</name>
  <files>src/summarizer.py, src/main.py</files>
  <action>Write summary outputs under `data/sources/YYYY-MM-DD` and route exceptions to `data/failed` while keeping per-item processing non-fatal.</action>
  <verify>
    <automated>python -m compileall src</automated>
  </verify>
  <done>Pipeline writes source summaries and safely records summarization failures.</done>
</task>

<task type="auto">
  <name>Task 2: Add summarizer unit tests</name>
  <files>tests/test_summarizer.py</files>
  <action>Add unit tests for summary path generation, source file creation, transcript extraction behavior, and failure-record behavior with mocked OpenRouter summarizer dependencies.</action>
  <verify>
    <automated>python -m unittest discover -s tests -p "test_*.py"</automated>
  </verify>
  <done>Test suite validates summary persistence and failure fallback behavior.</done>
</task>
</tasks>

<output>
After completion, create `.planning/phases/04-gemini-summarization/04-02-SUMMARY.md`
</output>
