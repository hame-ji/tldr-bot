---
phase: 02-telegram-polling-client
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_telegram_client.py
  - src/main.py
  - .github/workflows/digest.yml
autonomous: true
requirements:
  - POLL-05
  - POLL-06
must_haves:
  truths:
    - "URL extraction handles mixed surrounding text"
    - "Messages from non-allowed chats are ignored"
    - "Polling behavior is validated in CI"
  artifacts:
    - path: "tests/test_telegram_client.py"
      provides: "Regression tests for polling semantics"
    - path: "src/main.py"
      provides: "Entrypoint integration for polling"
    - path: ".github/workflows/digest.yml"
      provides: "Unit test execution in workflow"
---

<objective>
Add URL extraction and chat-id filtering verification with unit tests, and wire polling execution into the pipeline entrypoint.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add polling unit tests for extraction, offset, and chat filtering</name>
  <files>tests/test_telegram_client.py</files>
  <action>Create unit tests using mocks for API responses to verify regex extraction, offset semantics, and allowed-chat filtering behavior.</action>
  <verify>
    <automated>python -m unittest discover -s tests -p "test_*.py"</automated>
  </verify>
  <done>Unit tests verify URL regex extraction, offset semantics, and chat-id filtering behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Run polling from main and enforce tests in workflow</name>
  <files>src/main.py, .github/workflows/digest.yml</files>
  <action>Update `src/main.py` to execute poll flow from environment variables and print extracted URLs; add test execution step to workflow.</action>
  <verify>
    <automated>python -m compileall src && python -m unittest discover -s tests -p "test_*.py"</automated>
  </verify>
  <done>Entrypoint runs poll flow from env vars and workflow gates execution on test success.</done>
</task>
</tasks>

<output>
After completion, create `.planning/phases/02-telegram-polling-client/02-02-SUMMARY.md`
</output>
