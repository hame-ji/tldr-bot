---
phase: 01-infrastructure-bot-setup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/digest.yml
autonomous: true
requirements:
  - INFRA-04
  - INFRA-05
  - INFRA-06
  - INFRA-07
must_haves:
  truths:
    - "Daily and manual workflow triggers are available"
    - "Workflow has permissions and git history depth needed for push/amend behavior"
    - "Concurrent runs queue instead of canceling in-progress execution"
  artifacts:
    - path: ".github/workflows/digest.yml"
      provides: "Digest automation workflow definition"
      contains: "schedule and workflow_dispatch triggers"
  key_links:
    - from: ".github/workflows/digest.yml"
      to: "GITHUB_TOKEN push operations"
      via: "permissions: contents: write"
      pattern: "contents: write"
    - from: ".github/workflows/digest.yml"
      to: "future amend strategy"
      via: "checkout fetch-depth"
      pattern: "fetch-depth: 0"
---

<objective>
Define the GitHub Actions workflow that runs on schedule and manual dispatch with the exact permissions and checkout behavior required for repository writes.

Purpose: Make automation execution-ready and prevent known CI push/amend failure modes.
Output: Validated `digest.yml` workflow with correct trigger, permission, checkout, and concurrency settings.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create digest workflow with required triggers and runtime scaffold</name>
  <files>.github/workflows/digest.yml</files>
  <action>Create workflow with cron (`0 7 * * *`) plus `workflow_dispatch`, Python setup, dependency install, and placeholder execution command for the future pipeline entrypoint. Include basic guardrails (`set -e`) for deterministic failure behavior.</action>
  <verify>
    <automated>python - <<'PY'\nimport pathlib\ny = pathlib.Path('.github/workflows/digest.yml').read_text()\nassert 'cron: \"0 7 * * *\"' in y\nassert 'workflow_dispatch:' in y\nprint('triggers-ok')\nPY</automated>
  </verify>
  <done>`digest.yml` exists with both schedule and manual dispatch triggers and a runnable Python job skeleton.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce write permission, full checkout history, and queue-based concurrency</name>
  <files>.github/workflows/digest.yml</files>
  <action>Ensure workflow explicitly sets `permissions: contents: write`, checkout uses `fetch-depth: 0`, and `concurrency.cancel-in-progress` is `false`. Keep these values explicit and close to the top-level workflow config for easy review.</action>
  <verify>
    <automated>python - <<'PY'\nimport pathlib\ny = pathlib.Path('.github/workflows/digest.yml').read_text()\nrequired = ['contents: write', 'fetch-depth: 0', 'cancel-in-progress: false']\nmissing = [item for item in required if item not in y]\nassert not missing, f'missing: {missing}'\nprint('workflow-guards-ok')\nPY</automated>
  </verify>
  <done>Workflow file includes all required CI guardrails for commit/push behavior and non-canceling concurrency.</done>
</task>

</tasks>

<verification>
Validate trigger and guardrail strings in workflow file, then run a manual workflow dispatch in GitHub UI as a smoke check.
</verification>

<success_criteria>
- `digest.yml` includes daily cron and manual dispatch
- Workflow includes `permissions: contents: write`
- Checkout step uses `fetch-depth: 0`
- Concurrency uses `cancel-in-progress: false`
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-bot-setup/01-02-SUMMARY.md`
</output>
