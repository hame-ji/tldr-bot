---
phase: 01-infrastructure-bot-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - src/.gitkeep
  - data/.gitkeep
  - prompts/.gitkeep
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
must_haves:
  truths:
    - "Repository has required scaffold directories tracked in git"
    - "Dependency installation is deterministic from pinned requirements"
  artifacts:
    - path: "src/.gitkeep"
      provides: "Tracks src directory in repository"
    - path: "data/.gitkeep"
      provides: "Tracks data directory in repository"
    - path: "prompts/.gitkeep"
      provides: "Tracks prompts directory in repository"
    - path: "requirements.txt"
      provides: "Pinned Python dependency set"
  key_links:
    - from: "requirements.txt"
      to: "python -m pip install"
      via: "pinned versions"
      pattern: "=="
---

<objective>
Create the base repository scaffold and lock dependency versions so CI and local runs are reproducible from day one.

Purpose: Establish deterministic infrastructure before workflow and API integration tasks.
Output: Tracked directories plus pinned dependency manifest.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create required directory scaffold tracked in git</name>
  <files>src/.gitkeep, data/.gitkeep, prompts/.gitkeep</files>
  <action>Create `src/`, `data/`, and `prompts/` directories if missing and add `.gitkeep` placeholders so the scaffold is committed immediately. Do not create extra folders in this task.</action>
  <verify>
    <automated>test -d src && test -d data && test -d prompts && test -f src/.gitkeep && test -f data/.gitkeep && test -f prompts/.gitkeep</automated>
  </verify>
  <done>All three required directories exist and are represented by tracked placeholder files.</done>
</task>

<task type="auto">
  <name>Task 2: Add exact pinned Python dependencies for v1 baseline</name>
  <files>requirements.txt</files>
  <action>Create or update `requirements.txt` to include only the required v1 libraries with exact versions (`pyTelegramBotAPI`, `google-genai`, `trafilatura`, `requests`, `python-slugify`). Use `google-genai` and never `google-generativeai`.</action>
  <verify>
    <automated>python -m pip install -r requirements.txt</automated>
  </verify>
  <done>`requirements.txt` contains exact version pins for the five required dependencies and installs successfully.</done>
</task>

</tasks>

<verification>
Run `python -m pip install -r requirements.txt` and verify all scaffold directories are present in git status.
</verification>

<success_criteria>
- `src/`, `data/`, and `prompts/` exist and are tracked
- `requirements.txt` pins all required dependencies exactly
- Dependency installation succeeds without manual package edits
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-bot-setup/01-01-SUMMARY.md`
</output>
