---
phase: 03-content-fetching
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content_fetcher.py
  - src/main.py
  - tests/test_content_fetcher.py
autonomous: true
requirements:
  - FETCH-04
must_haves:
  truths:
    - "Fetch failures produce file records under data/failed/<date>/"
    - "Per-URL failures do not abort batch processing"
    - "Failure behavior is protected by unit tests"
  artifacts:
    - path: "tests/test_content_fetcher.py"
      provides: "Regression coverage for timeout/failure behavior"
    - path: "src/main.py"
      provides: "Entrypoint integration for fetch step"
---

<objective>
Add failure-record behavior and test coverage so fetch errors are durable and non-fatal to the run.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Implement failure record writer and batch-safe fetch loop</name>
  <files>src/content_fetcher.py, src/main.py</files>
  <action>Add failure markdown writer under `data/failed/YYYY-MM-DD` and ensure each URL is processed independently so failures are captured and execution continues.</action>
  <verify>
    <automated>python -m compileall src</automated>
  </verify>
  <done>Failures are stored on disk and fetch loop returns structured success/failure results without crashing.</done>
</task>

<task type="auto">
  <name>Task 2: Add content-fetcher unit tests</name>
  <files>tests/test_content_fetcher.py</files>
  <action>Add tests for classification, timeout usage, and failure record creation with mocks for HTTP and extraction calls.</action>
  <verify>
    <automated>python -m unittest discover -s tests -p "test_*.py"</automated>
  </verify>
  <done>Test suite covers fetch timeout and failure-record behavior for regression safety.</done>
</task>
</tasks>

<output>
After completion, create `.planning/phases/03-content-fetching/03-02-SUMMARY.md`
</output>
