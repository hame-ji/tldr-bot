---
phase: 05-digest-generation-delivery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/digest_generator.py
  - src/main.py
  - prompts/digest.txt
  - tests/test_digest_generator.py
autonomous: true
requirements:
  - DGST-01
  - DGST-02
  - DGST-03
must_haves:
  truths:
    - "Daily digest markdown is written to data/digests/<date>.md"
    - "Digest format is controlled by prompts/digest.txt"
    - "Digest includes a failed URLs section when failures exist"
  artifacts:
    - path: "src/digest_generator.py"
      provides: "Digest assembly and file write behavior"
    - path: "tests/test_digest_generator.py"
      provides: "Regression coverage for digest generation rules"
---

<objective>
Implement digest generation module that assembles success + failure content into a dated Markdown digest file using a prompt file for format control.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add digest generator module and prompt configuration</name>
  <files>src/digest_generator.py, prompts/digest.txt</files>
  <action>Create digest generation entrypoint that accepts summarized items, loads `prompts/digest.txt`, renders digest markdown, and writes output to `data/digests/YYYY-MM-DD.md`.</action>
  <verify>
    <automated>python -m compileall src</automated>
  </verify>
  <done>Digest module writes deterministic daily markdown output and prompt file controls output structure.</done>
</task>

<task type="auto">
  <name>Task 2: Add failed-URL section and generation tests</name>
  <files>src/digest_generator.py, tests/test_digest_generator.py</files>
  <action>Render a dedicated failed URLs section sourced from failed pipeline results and add tests for file path, prompt loading, and failure section inclusion.</action>
  <verify>
    <automated>python -m unittest discover -s tests -p "test_*.py"</automated>
  </verify>
  <done>Tests verify digest write behavior, prompt control, and failed URL visibility.</done>
</task>
</tasks>

<output>
After completion, create `.planning/phases/05-digest-generation-delivery/05-01-SUMMARY.md`
</output>
