---
phase: 06-pipeline-orchestration-git-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - .github/workflows/digest.yml
  - tests/test_workflow_commit_strategy.py
  - architecture.md
autonomous: true
requirements:
  - STOR-02
  - STOR-03
  - STOR-04
must_haves:
  truths:
    - "Workflow creates one digest commit per UTC day when outputs changed"
    - "Same-day reruns amend existing digest commit instead of adding a duplicate"
    - "Amend path pushes with --force-with-lease"
    - "No commit/push occurs when pipeline reports zero processed URLs"
  artifacts:
    - path: ".github/workflows/digest.yml"
      provides: "Commit mode decision tree (create/amend/skip)"
    - path: "tests/test_workflow_commit_strategy.py"
      provides: "Regression coverage for commit strategy rules"
---

<objective>
Implement GitHub Actions commit/push strategy for daily outputs: create on first run of day, amend on rerun, and skip entirely on empty-day runs.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add commit strategy decision logic to digest workflow</name>
  <files>.github/workflows/digest.yml</files>
  <action>Add workflow steps that parse pipeline run outcome, stage output files, skip when no eligible changes, create date-keyed commit for first run, amend on same-day rerun, and push safely (`--force-with-lease` for amend path).</action>
  <verify>
    <automated>python3 -m unittest discover -s tests -p "test_*.py"</automated>
  </verify>
  <done>Workflow enforces one-commit-per-day behavior and safe amend push semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Add commit-strategy tests and operator docs</name>
  <files>tests/test_workflow_commit_strategy.py, architecture.md</files>
  <action>Add tests for create/amend/skip branch logic and document expected behavior for cron/manual reruns and empty-day no-op runs.</action>
  <verify>
    <automated>python3 -m unittest discover -s tests -p "test_*.py"</automated>
  </verify>
  <done>Commit strategy is regression-tested and documented for safe operations.</done>
</task>
</tasks>

<output>
After completion, create `.planning/phases/06-pipeline-orchestration-git-integration/06-02-SUMMARY.md`
</output>
