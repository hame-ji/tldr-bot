---
phase: 02-telegram-polling-client
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/telegram_client.py
  - state.json
autonomous: true
requirements:
  - POLL-01
  - POLL-02
  - POLL-03
  - POLL-04
must_haves:
  truths:
    - "Polling fetches updates from Telegram at run start"
    - "Offset persistence uses last update id plus one"
    - "Offset state is written before downstream processing"
  artifacts:
    - path: "src/telegram_client.py"
      provides: "Polling and offset primitives"
    - path: "state.json"
      provides: "Persisted telegram offset state"
---

<objective>
Implement Telegram polling primitives and offset persistence so each run can fetch only new updates and checkpoint the cursor in `state.json`.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Implement Telegram API polling client</name>
  <files>src/telegram_client.py</files>
  <action>Add `get_updates` and `poll_urls` logic with API calls to `getUpdates`, offset propagation, and deterministic return payload for downstream pipeline stages.</action>
  <verify>
    <automated>python -m compileall src</automated>
  </verify>
  <done>Polling logic is implemented with `getUpdates` and returns deterministic update metadata for downstream stages.</done>
</task>

<task type="auto">
  <name>Task 2: Persist polling cursor in state.json</name>
  <files>src/telegram_client.py, state.json</files>
  <action>Add `load_offset`/`save_offset` helpers and initialize `state.json` schema for first-run behavior.</action>
  <verify>
    <automated>python -m unittest discover -s tests -p "test_*.py"</automated>
  </verify>
  <done>Offset is read/written through `state.json` and persists as `max(update_id) + 1` on new updates.</done>
</task>
</tasks>

<output>
After completion, create `.planning/phases/02-telegram-polling-client/02-01-SUMMARY.md`
</output>
