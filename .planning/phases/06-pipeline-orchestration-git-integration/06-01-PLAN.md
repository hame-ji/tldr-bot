---
phase: 06-pipeline-orchestration-git-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main.py
  - tests/test_main.py
  - tests/test_telegram_client.py
autonomous: true
requirements:
  - STOR-01
must_haves:
  truths:
    - "Pipeline emits deterministic run outcome metadata for downstream commit gating"
    - "Markdown artifacts remain the only durable storage outputs for summaries and digests"
    - "Orchestration behavior is covered by unit tests"
  artifacts:
    - path: "src/main.py"
      provides: "End-to-end orchestration outcome and empty-day semantics"
    - path: "tests/test_main.py"
      provides: "Regression coverage for orchestration outcome behavior"
---

<objective>
Harden Python orchestration so the workflow can reliably decide whether to commit, while preserving markdown-only storage behavior.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add structured run outcome from pipeline entrypoint</name>
  <files>src/main.py</files>
  <action>Refactor entrypoint flow to compute and expose run outcome fields (processed URL count, digest path created flag, and send count) in deterministic stdout or return structure usable by workflow steps.</action>
  <verify>
    <automated>python3 -m compileall src</automated>
  </verify>
  <done>Pipeline emits stable run outcome values that downstream workflow logic can consume for commit/no-commit decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Add orchestration unit tests for outcome and empty-day behavior</name>
  <files>tests/test_main.py, tests/test_telegram_client.py</files>
  <action>Add tests that validate run outcome fields, empty-day no-op behavior, and markdown-output assumptions without requiring live Telegram/Gemini calls.</action>
  <verify>
    <automated>python3 -m unittest discover -s tests -p "test_*.py"</automated>
  </verify>
  <done>Tests protect output gating semantics required for phase-6 git integration.</done>
</task>
</tasks>

<output>
After completion, create `.planning/phases/06-pipeline-orchestration-git-integration/06-01-SUMMARY.md`
</output>
