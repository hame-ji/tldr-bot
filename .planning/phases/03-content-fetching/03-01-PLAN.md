---
phase: 03-content-fetching
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content_fetcher.py
autonomous: true
requirements:
  - FETCH-01
  - FETCH-02
  - FETCH-03
must_haves:
  truths:
    - "URL classification separates YouTube from article URLs"
    - "Article fetch path uses explicit request timeout"
    - "Extraction logic returns cleaned text or raises explicit error"
  artifacts:
    - path: "src/content_fetcher.py"
      provides: "Content classification and article extraction primitives"
---

<objective>
Implement core content fetching primitives for URL classification and article extraction with mandatory timeout safety.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Implement URL classification and article extraction</name>
  <files>src/content_fetcher.py</files>
  <action>Create `classify_url`, `fetch_article_text`, and `fetch_url` primitives that classify YouTube links and fetch article text using requests + trafilatura.</action>
  <verify>
    <automated>python -m compileall src</automated>
  </verify>
  <done>Content fetch module classifies URL types and extracts article text with explicit HTTP timeout.</done>
</task>
</tasks>

<output>
After completion, create `.planning/phases/03-content-fetching/03-01-SUMMARY.md`
</output>
