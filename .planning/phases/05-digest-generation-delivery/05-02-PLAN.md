---
phase: 05-digest-generation-delivery
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/telegram_client.py
  - src/main.py
  - tests/test_telegram_client.py
autonomous: true
requirements:
  - DGST-04
  - DGST-05
  - DGST-06
must_haves:
  truths:
    - "Digest delivery sends to configured Telegram chat"
    - "Digest messages are chunked by paragraph and each chunk is <= 4096 chars"
    - "Empty-day runs skip digest delivery and skip digest file generation"
  artifacts:
    - path: "src/telegram_client.py"
      provides: "Chunked Telegram send helper and parse mode behavior"
    - path: "src/main.py"
      provides: "Orchestration guard for empty-day no-op"
    - path: "tests/test_telegram_client.py"
      provides: "Regression coverage for chunking and empty-day guard"
---

<objective>
Integrate digest delivery into Telegram with safe chunking, and add an orchestrator guard that exits cleanly when no URLs were processed.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add Telegram digest delivery and chunking utility</name>
  <files>src/telegram_client.py, src/main.py</files>
  <action>Add delivery function that sends digest text to Telegram chat with `parse_mode="HTML"`, splits content at paragraph boundaries to remain within 4096 characters, and preserves chunk order.</action>
  <verify>
    <automated>python -m compileall src</automated>
  </verify>
  <done>Long digests are delivered as ordered multi-message output without Telegram length errors.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce empty-day guard and add delivery tests</name>
  <files>src/main.py, tests/test_telegram_client.py</files>
  <action>Add early-return path when there are zero processed URLs so digest write/send are skipped, and add tests for chunk sizing, paragraph splitting, and no-send behavior on empty days.</action>
  <verify>
    <automated>python -m unittest discover -s tests -p "test_*.py"</automated>
  </verify>
  <done>Pipeline safely no-ops on empty days and tests protect delivery behavior.</done>
</task>
</tasks>

<output>
After completion, create `.planning/phases/05-digest-generation-delivery/05-02-SUMMARY.md`
</output>
